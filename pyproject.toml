[project]
name = "original-media-downloader"
version = "1.0.0"
description = "A local web GUI for downloading original media from creator profiles."
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
